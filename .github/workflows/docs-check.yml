name: Documentation Check

on:
  pull_request:
  workflow_dispatch:

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify docs
        run: |
          test -f README.md
          test -f docs/ECOSYSTEM.md
